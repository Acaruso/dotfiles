
" Enable 256 colors
set t_Co=256

" Set colorscheme
color ir_black

" Show matching brackets when text indicator is over them
set showmatch

" How many tenths of a second to blink when matching brackets
set mat=2

" Bind jk to Esc
inoremap jk <Esc>

" Enable syntax highlighting
syntax enable

"Use spaces instead of tabs
set expandtab

" Be smart when using tabs
set smarttab

" Set Tab to 4 Spaces 
set shiftwidth=4
set tabstop=4

set ai "Auto indent
set si "Smart indent
set wrap "Wrap lines

" Cursor can move past last character
set virtualedit=onemore

" Turn on line numbers
set number
